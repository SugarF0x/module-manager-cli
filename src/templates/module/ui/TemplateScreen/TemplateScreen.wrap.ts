import TemplateScreen, { ITemplateScreenActions, ITemplateScreenState } from './TemplateScreen.js'
import { connect } from "react-redux"

const mapStateToProps = (): ITemplateScreenState => ({

})

const mapDispatchToProps = (): ITemplateScreenActions => ({

})

export default connect(mapStateToProps, mapDispatchToProps)(TemplateScreen)
