import ModuleScreen, { IModuleActions, IModuleState } from './ModuleScreen.js'
import { connect } from "react-redux"

const mapStateToProps = (): IModuleState => ({

})

const mapDispatchToProps = (): IModuleActions => ({

})

export default connect(mapStateToProps, mapDispatchToProps)(ModuleScreen)
